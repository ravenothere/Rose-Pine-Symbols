const PKG_PROP_MAP = {
  "rose-pine-symbols.folderStyle": "folderStyle",
};

const MESSAGES = {
  needsReload: "Rosé Pine Symbols: Reload window to apply the new folder style.",
  reloadButton: "Reload Now",
};

module.exports = { PKG_PROP_MAP, MESSAGES };
